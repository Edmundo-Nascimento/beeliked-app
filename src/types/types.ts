export type ButtonProps = {
  label: string;
  onClick: any;
}

export type CheckboxProps = {
  checked: boolean;
  onClick?: any;
  onChange: any;
}

//TODO: Create all generic types and others in other files to type correctly all the function, params and others
//TODO:Create types and interfaces to replace all "any" type